# 文件批量重命名

```python
# 遍历文件夹更改文件后缀为png
import os
path = 'hello'
for root, dirs, files in os.walk(path):
    for file in files:
        if file.endswith('.jp2'):
            os.rename(os.path.join(root, file), os.path.join(root, file[:-4] + '.png'))
```